hight = float(input("enter the height of the triangle:"))
base = float(input("enter the base of the triangl:"))

hight_or_base = hight * base
area = hight_or_base * 0.5 # use (1/2) or *0.5
print(area)